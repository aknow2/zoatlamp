# AC1004 — Phase 4: Frame Extraction

## 前提条件
- `npm run dev` でアプリが起動していること
- ブラウザで `http://localhost:5173` にアクセスできること
- テスト用動画ファイル（mp4）が手元にあること

## テスト手順

### TC-004-01: 生成ボタンで指定フレーム数分のフレームが取得される
1. 動画ファイルを選択してプレビューを表示する
2. 動画プレビュー上で基準点を指定する
3. `frameCountInput` を `5` に設定する
4. `frameIntervalInput` を `0.1` に設定する
5. `generateButton` をクリックする

**Expected:**
- エラー表示が出ない
- `state.frames` 相当の抽出結果が `5` 件になる
- 各フレームに `index` と `time` が設定される

### TC-004-02: 基準点未指定では生成せずエラーを表示する
1. 動画ファイルを選択してプレビューを表示する
2. 基準点を指定しない
3. `generateButton` をクリックする

**Expected:**
- `動画上で切り出し基準点を指定してください。` と表示される
- フレーム抽出は実行されない

### TC-004-03: フレーム範囲が動画長を超える場合は生成せずエラーを表示する
1. 動画ファイルを選択してプレビューを表示する
2. 動画プレビュー上で基準点を指定する
3. `startSecondInput` と `frameCountInput` と `frameIntervalInput` を、最終フレーム時刻が動画時間を超える値に設定する
4. `generateButton` をクリックする

**Expected:**
- `指定したフレーム範囲が動画の長さを超えています。` と表示される
- フレーム抽出は実行されない

### TC-004-04: 入力値不正時に対応するバリデーションエラーを表示する
1. 動画ファイルを選択してプレビューを表示する
2. 動画プレビュー上で基準点を指定する
3. `frameCountInput` を `2` に設定して `generateButton` をクリックする
4. `frameIntervalInput`、`startSecondInput`、`sliceLengthInput`、`outputRadiusInput` も不正値で確認する

**Expected:**
- 各不正値に対応したエラーメッセージが表示される
- フレーム抽出は実行されない

## 結果記録

| TC | 結果 | 備考 |
|---|---|---|
| TC-004-01 | Pass | `createImageBitmap` 呼び出し5件、bitmap=320x240、seekTimes=`[0,0.1,0.2,0.30000000000000004,0.4]`、エラー表示なし、downloadButton enabled |
| TC-004-02 | Pass | `動画上で切り出し基準点を指定してください。` を表示、bitmap抽出0件、downloadButton disabled |
| TC-004-03 | Pass | `指定したフレーム範囲が動画の長さを超えています。` を表示、bitmap抽出0件 |
| TC-004-04 | Pass | frameCount/frameInterval/startSecond/sliceLength/outputRadius の各不正値で期待エラーを確認、bitmap抽出0件 |

### 実行ログ（2026-05-18）

- `npm test`: pass（2 files / 15 tests）
- `npm run dev -- --host 127.0.0.1 --port 5173`: 5173 に既存サーバー、追加サーバーは 5174 で起動。AC1004 は `http://127.0.0.1:5173/` で実行
- テスト動画: `ffmpeg` で `/private/tmp/ac1004-test.mp4` を生成（2秒、320x240、30fps）
- ブラウザ受け入れテスト: Chrome DevTools Protocol で AC1004 の TC-004-01〜TC-004-04 を実行し、4 passed
